@model IEnumerable<BeatLab.Type_Music>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name_Type_Music)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name_Type_Music)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID_Type_music }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID_Type_music }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID_Type_music })
        </td>
    </tr>
}

</table>
