@model BeatLab.Models.Entities.Type_Music

@{
    ViewBag.Title = "Редактирование типа музыки";
}
<br />

<h3>Редактирование типа музыки</h3>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="form-horizontal">

        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.ID_Type_music)

    <div class="form-group">
        <label for="name-4c18" class="control-label col-md-2">Название типа музыки</label>
        <div class="col-md-10">
            @Html.EditorFor(model => model.Name_Type_Music, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.Name_Type_Music, "", new { @class = "text-danger" })
        </div>
    </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">
                <input type="submit" value="Сохранить" class="btn btn-default" />
            </div>
        </div>
    </div>
}
